startCommand:
  type: http
  url: https://networklytics.net/api/v1/mcp/
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "NetworkLytics API key — required only for private analysis access (get_analysis_by_id). Get yours at https://networklytics.net/accounts/settings/"
    required: []
  exampleConfig:
    apiKey: "nly_your_api_key_here"
